<div class="row">
  <div class="col-sm-6 col-sm-offset-3">
    <form method="POST" action="/articles/{{note.articleId}}/notes/{{note._id}}?_method=PUT">
        {{> note-form}}
        <!-- BUTTON -->
        <div class="form-group">
            <button class="btn btn-primary" type="submit">Keep Tea</button>
        </div>
    </form>
  </div>
</div>
